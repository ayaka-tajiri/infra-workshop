apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: infra-workshop
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
    - hosts:
        - tajiri.she-dev.site
      secretName: infra-workshop-tls
  rules:
    - host: "tajiri.she-dev.site"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: infra-workshop
                port:
                  number: 80

